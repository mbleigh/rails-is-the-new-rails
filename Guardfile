# A sample Guardfile
# More info at https://github.com/guard/guard#readme

# This is an example with all options that you can specify for guard-process
guard 'process', :name => 'keydown', :command => 'keydown slides index.md' do
  watch('index.md')
end

